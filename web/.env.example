# ── API Base URL (backend) ─────────────────────────────
# Leave empty to use current page host (e.g. http://192.168.89.61:3000 when opened from that IP).
# Set for explicit URL, e.g. http://localhost:3000 or http://192.168.89.61:3000
# VITE_API_BASE_URL=http://localhost:3000
