/**
 * App toast API — visual defaults live on <Toaster /> in App.tsx
 * (position, duration, rich colors, no close button). Import from here
 * instead of "sonner" so behaviour stays consistent.
 */
export { toast, type ExternalToast, type ToastT } from "sonner";
