# Spec 12 – Performance

- **API:** Pagination and filters for list endpoints; avoid unbounded queries.
- **Frontend:** React Query for caching and deduplication; lazy routes for admin; avoid full icon set import.
- **Offline:** Queue replay in order; batch or throttle if needed per product decision.
